<!DOCTYPE html>
<html>
  <head>
    <title>Ramen App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 text-gray-800 leading-relaxed font-sans">
    <nav class="bg-white shadow-md sticky top-0 z-50">
      <div class="max-w-6xl mx-auto flex justify-between items-center px-5 h-[70px]">
        <a href="<%= root_path %>" class="text-2xl font-bold text-gray-800 flex items-center">🍜 Ramen Tracker</a>

        <div class="flex items-center gap-8">
          <a href="<%= root_path %>" class="text-gray-600 font-medium hover:text-purple-600 transition-colors">Home</a>
          <% if logged_in? %>
            <a href="<%= my_visits_path %>" class="text-gray-600 font-medium hover:text-purple-600 transition-colors">My Visits</a>
            <a href="<%= rankings_path %>" class="text-gray-600 font-medium hover:text-purple-600 transition-colors">Top Rankings</a>
            <div class="flex items-center gap-4">
              <span class="text-gray-600 text-sm"><%= current_user.name %></span>
              <%= button_to "Sign out", logout_path, method: :delete, class: "inline-flex items-center justify-center px-5 py-2.5 rounded text-sm font-medium bg-gray-100 text-gray-800 border border-gray-300 hover:bg-gray-200 transition-all cursor-pointer" %>
            </div>
          <% else %>
            <a href="<%= rankings_path %>" class="text-gray-600 font-medium hover:text-purple-600 transition-colors">Top Rankings</a>
            <a href="<%= login_path %>" class="text-gray-600 font-medium hover:text-purple-600 transition-colors">Sign in</a>
          <% end %>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto min-h-[calc(100vh-140px)] p-5">
      <% if notice.present? %>
        <div class="p-4 rounded bg-green-100 border border-green-300 text-green-800 mb-5 mt-5">
          <%= notice %>
        </div>
      <% end %>

      <% if alert.present? %>
        <div class="p-4 rounded bg-red-100 border border-red-300 text-red-800 mb-5 mt-5">
          <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="bg-gray-800 text-white text-center py-5 mt-10">
      <p>&copy; 2024 Ramen Tracker. Built with Rails and Hotwire.</p>
    </footer>
  </body>
</html>
